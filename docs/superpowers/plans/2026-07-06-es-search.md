# ES 全文搜索 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完全复刻 Payara IndexerManagerBean 的实时索引 + ES 搜索，替代当前 DB LIKE 搜索。

**Architecture:** 单 `indexer_manager.py` + 各 CRUD 方法内联调用。ES 6.6.1 不变，Python client `elasticsearch==6.8.2`。搜索 ES 优先 + DB fallback。索引失败静默日志不抛异常。

**Tech Stack:** Python 3.11 + FastAPI + SQLAlchemy + Elasticsearch 6.6.1 + elasticsearch-py 6.8.2

## Global Constraints

- ES 6.6.1 容器不变（es:9200）
- 索引命名：`parts-{workspace_id}` / `documents-{workspace_id}`
- 索引映射对齐 spec 定义
- 所有索引操作 try/except 静默失败（不 raise）
- 搜索 ES 失败 → fallback 现有 DB LIKE 查询
- 文件命名对齐 Payara Bean 命名（IndexerManagerBean → indexer_manager.py）
- 测试全 Mock Elasticsearch 客户端

---

### Task 1: Setup——依赖 + 配置

**Files:**
- Modify: `requirements.txt`（+1）
- Modify: `app/core/config.py`（+1）

- [ ] **Step 1: Add elasticsearch dependency**

```python
# requirements.txt — add at end
elasticsearch==6.8.2
```

- [ ] **Step 2: Add ES_URL config**

```python
# app/core/config.py — add after VAULT_PATH
ES_URL: str = "http://es:9200"
```

- [ ] **Step 3: Install and verify**

Run: `source venv/bin/activate && pip install elasticsearch==6.8.2 -q && python -c "from elasticsearch import Elasticsearch; print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add requirements.txt app/core/config.py
git commit -m "chore: ES search——elasticsearch==6.8.2 + ES_URL config"
```

---

### Task 2: `indexer_manager.py`——ES 连接 + 索引管理 + 文档 CRUD

**Files:**
- Create: `app/services/indexer_manager.py`

- [ ] **Step 1: Create the file with full implementation**

```python
"""ES 索引管理 + 实时索引——对标 Payara IndexerManagerBean。"""
import logging
from elasticsearch import Elasticsearch
from elasticsearch.helpers import bulk
from app.core.config import settings

logger = logging.getLogger(__name__)

PART_MAPPING = {
    "partNumber": {"type": "text", "analyzer": "simple"},
    "partName": {"type": "text"},
    "partKey": {"type": "keyword"},
    "version": {"type": "keyword"},
    "iteration": {"type": "integer"},
    "description": {"type": "text"},
    "revisionNote": {"type": "text"},
    "authorLogin": {"type": "keyword"},
    "authorName": {"type": "keyword"},
    "creationDate": {"type": "date"},
    "modificationDate": {"type": "date"},
    "standardPart": {"type": "boolean"},
    "workspaceId": {"type": "keyword"},
}

DOC_MAPPING = {
    "docKey": {"type": "keyword"},
    "docMasterId": {"type": "keyword"},
    "title": {"type": "text"},
    "version": {"type": "keyword"},
    "iteration": {"type": "integer"},
    "description": {"type": "text"},
    "revisionNote": {"type": "text"},
    "authorLogin": {"type": "keyword"},
    "authorName": {"type": "keyword"},
    "creationDate": {"type": "date"},
    "modificationDate": {"type": "date"},
    "workspaceId": {"type": "keyword"},
}


class IndexerManager:
    def __init__(self):
        self._es = None

    @property
    def es(self):
        if self._es is None:
            self._es = Elasticsearch([settings.ES_URL])
        return self._es

    def _part_index_name(self, ws: str) -> str:
        return f"parts-{ws.lower()}"

    def _doc_index_name(self, ws: str) -> str:
        return f"documents-{ws.lower()}"

    def create_index(self, ws: str):
        try:
            for prefix, mapping in [(_part_index_name, PART_MAPPING), (_doc_index_name, DOC_MAPPING)]:
                idx = prefix(ws)
                if not self.es.indices.exists(index=idx):
                    self.es.indices.create(index=idx, body={"mappings": {"_doc": {"properties": mapping}}})
        except Exception:
            logger.warning(f"Failed to create ES index for workspace {ws}", exc_info=True)

    def delete_index(self, ws: str):
        try:
            for prefix in [_part_index_name, _doc_index_name]:
                idx = prefix(ws)
                if self.es.indices.exists(index=idx):
                    self.es.indices.delete(index=idx)
        except Exception:
            logger.warning(f"Failed to delete ES index for workspace {ws}", exc_info=True)

    def _part_to_doc(self, pr) -> dict:
        """PartRevision → ES document."""
        it = pr.last_iteration
        pm = pr.part_master
        return {
            "partKey": f"{pm.number}-{pr.version}",
            "partNumber": pm.number,
            "partName": pm.name,
            "version": pr.version,
            "iteration": it.iteration if it else 0,
            "description": pr.description or "",
            "revisionNote": it.revision_note if it else "",
            "authorLogin": pr.author_login or "",
            "authorName": pr.author_login or "",
            "creationDate": pr.creation_date.isoformat() if pr.creation_date else None,
            "modificationDate": it.modification_date.isoformat() if it and it.modification_date else None,
            "standardPart": pm.standard_part or False,
            "workspaceId": pr.workspace_id,
        }

    def _doc_to_es_doc(self, dr) -> dict:
        """DocumentRevision → ES document."""
        it = dr.last_iteration
        return {
            "docKey": f"{dr.documentmaster_id}-{dr.version}",
            "docMasterId": dr.documentmaster_id,
            "title": dr.title or "",
            "version": dr.version,
            "iteration": it.iteration if it else 0,
            "description": dr.description or "",
            "revisionNote": it.revision_note if it else "",
            "authorLogin": dr.author_login or "",
            "authorName": dr.author_login or "",
            "creationDate": dr.creation_date.isoformat() if dr.creation_date else None,
            "modificationDate": it.modification_date.isoformat() if it and it.modification_date else None,
            "workspaceId": dr.workspace_id,
        }

    def index_part_revision(self, pr):
        try:
            idx = self._part_index_name(pr.workspace_id)
            doc = self._part_to_doc(pr)
            self.es.index(index=idx, doc_type="_doc", id=doc["partKey"], body=doc)
        except Exception:
            logger.warning(f"ES index failed for part {pr.partmaster_partnumber}-{pr.version}", exc_info=True)

    def delete_part_revision(self, key: str, ws: str):
        try:
            idx = self._part_index_name(ws)
            self.es.delete(index=idx, doc_type="_doc", id=key, ignore=[404])
        except Exception:
            logger.warning(f"ES delete failed for part {key}", exc_info=True)

    def index_document_revision(self, dr):
        try:
            idx = self._doc_index_name(dr.workspace_id)
            doc = self._doc_to_es_doc(dr)
            self.es.index(index=idx, doc_type="_doc", id=doc["docKey"], body=doc)
        except Exception:
            logger.warning(f"ES index failed for document {dr.documentmaster_id}-{dr.version}", exc_info=True)

    def delete_document_revision(self, key: str, ws: str):
        try:
            idx = self._doc_index_name(ws)
            self.es.delete(index=idx, doc_type="_doc", id=key, ignore=[404])
        except Exception:
            logger.warning(f"ES delete failed for document {key}", exc_info=True)

    def reindex_all(self, db, ws: str) -> dict:
        """全量重建索引。先删后建，从 DB 全量扫描。"""
        from app.models.part import PartRevision
        from app.models.document import DocumentRevision
        from sqlalchemy.orm import joinedload

        self.delete_index(ws)
        self.create_index(ws)

        parts = db.query(PartRevision).options(
            joinedload(PartRevision.iterations),
            joinedload(PartRevision.part_master),
        ).filter(PartRevision.workspace_id == ws).all()

        docs = db.query(DocumentRevision).options(
            joinedload(DocumentRevision.iterations),
        ).filter(DocumentRevision.workspace_id == ws).all()

        count_parts = 0
        count_docs = 0
        try:
            actions = []
            for pr in parts:
                doc = self._part_to_doc(pr)
                actions.append({"_index": self._part_index_name(ws), "_type": "_doc", "_id": doc["partKey"], "_source": doc})
            if actions:
                bulk(self.es, actions)
            count_parts = len(actions)

            actions = []
            for dr in docs:
                doc = self._doc_to_es_doc(dr)
                actions.append({"_index": self._doc_index_name(ws), "_type": "_doc", "_id": doc["docKey"], "_source": doc})
            if actions:
                bulk(self.es, actions)
            count_docs = len(actions)
        except Exception:
            logger.warning(f"ES reindex failed for workspace {ws}", exc_info=True)

        return {"parts": count_parts, "documents": count_docs}


indexer_manager = IndexerManager()
```

- [ ] **Step 2: Verify import**

Run: `source venv/bin/activate && python -c "from app.services.indexer_manager import indexer_manager; print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add app/services/indexer_manager.py
git commit -m "feat: ES搜索——indexer_manager.py（ES连接+索引管理+文档CRUD+reindex）"
```

---

### Task 3: `es_query_builder.py`——搜索 Query 构建

**Files:**
- Create: `app/services/es_query_builder.py`

- [ ] **Step 1: Create the file**

```python
"""ES 搜索 Query 构建——对标 Payara SearchQuery/PartSearchQuery/DocumentSearchQuery。"""


class EsQueryBuilder:
    def __init__(self):
        from app.services.indexer_manager import indexer_manager as im
        self.im = im

    def search_parts(self, ws: str, params: dict) -> list[str]:
        """返回匹配的 partKey 列表。"""
        idx = self.im._part_index_name(ws)
        must = []
        if params.get("q"):
            must.append({"multi_match": {"query": params["q"], "fields": ["partNumber^2", "partName", "description"]}})
        if params.get("number"):
            must.append({"match": {"partNumber": params["number"]}})
        if params.get("name"):
            must.append({"match": {"partName": params["name"]}})
        if params.get("version"):
            must.append({"term": {"version": params["version"]}})
        if params.get("author"):
            must.append({"term": {"authorLogin": params["author"]}})
        body = {"query": {"bool": {"must": must}}} if must else {"query": {"match_all": {}}}
        body["from"] = params.get("from", 0)
        body["size"] = params.get("size", 20)
        result = self.im.es.search(index=idx, body=body, _source=["partKey"])
        return [h["_source"]["partKey"] for h in result["hits"]["hits"]]

    def search_documents(self, ws: str, params: dict) -> list[str]:
        """返回匹配的 docKey 列表。"""
        idx = self.im._doc_index_name(ws)
        must = []
        if params.get("q"):
            must.append({"multi_match": {"query": params["q"], "fields": ["docMasterId^2", "title"]}})
        if params.get("id"):
            must.append({"match": {"docMasterId": params["id"]}})
        if params.get("title"):
            must.append({"match": {"title": params["title"]}})
        if params.get("version"):
            must.append({"term": {"version": params["version"]}})
        if params.get("author"):
            must.append({"term": {"authorLogin": params["author"]}})
        body = {"query": {"bool": {"must": must}}} if must else {"query": {"match_all": {}}}
        body["from"] = params.get("from", 0)
        body["size"] = params.get("size", 20)
        result = self.im.es.search(index=idx, body=body, _source=["docKey"])
        return [h["_source"]["docKey"] for h in result["hits"]["hits"]]


es_query_builder = EsQueryBuilder()
```

- [ ] **Step 2: Verify import**

Run: `source venv/bin/activate && python -c "from app.services.es_query_builder import es_query_builder; print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add app/services/es_query_builder.py
git commit -m "feat: ES搜索——es_query_builder.py（parts/documents搜索query构建）"
```

---

### Task 4: `product_manager.py`——6 处实时索引

**Files:**
- Modify: `app/services/product_manager.py`

**Interfaces:**
- Consumes: `app/services/indexer_manager.py`（`indexer_manager`）

- [ ] **Step 1: Add import at top**

```python
from app.services.indexer_manager import indexer_manager
```

- [ ] **Step 2: Add index calls in 6 methods**

| Method | Line (after this action) | Code |
|--------|-------------------------|------|
| `create_part` | after `db.commit()` | `indexer_manager.index_part_revision(revision)` |
| `checkin` | after `db.commit()` | `indexer_manager.index_part_revision(pr)` |
| `update_iteration` | after `db.commit()` | `indexer_manager.index_part_revision(pr)` |
| `release` | after `db.commit()` | `indexer_manager.index_part_revision(pr)` |
| `mark_obsolete` | after `db.commit()` | `indexer_manager.index_part_revision(pr)` |
| `delete_revision` | before `db.delete(pr)` | `indexer_manager.delete_part_revision(f"{number}-{version}", workspace_id)` |

- [ ] **Step 3: Run regression tests**

Run: `source venv/bin/activate && pytest tests/test_product_service.py tests/test_parts_api.py -q`
Expected: all pass

- [ ] **Step 4: Commit**

```bash
git add app/services/product_manager.py
git commit -m "feat: ES搜索——product_manager 6处实时索引（create/checkin/update/release/obsolete/delete）"
```

---

### Task 5: `document_manager.py`——5 处实时索引

**Files:**
- Modify: `app/services/document_manager.py`

**Interfaces:**
- Consumes: `app/services/indexer_manager.py`

- [ ] **Step 1: Add import**

```python
from app.services.indexer_manager import indexer_manager
```

- [ ] **Step 2: Add index calls in 5 methods**

| Method | Code |
|--------|------|
| `create_document` | `indexer_manager.index_document_revision(dr)` |
| `checkin` | `indexer_manager.index_document_revision(dr)` |
| `set_tags` | `indexer_manager.index_document_revision(dr)` |
| `release` | `indexer_manager.index_document_revision(dr)` |
| `delete_revision` | `indexer_manager.delete_document_revision(f"{doc_id}-{ver}", ws)` |

- [ ] **Step 3: Run regression tests**

Run: `source venv/bin/activate && pytest tests/test_document_service.py tests/test_documents_api.py -q`
Expected: all pass

- [ ] **Step 4: Commit**

```bash
git add app/services/document_manager.py
git commit -m "feat: ES搜索——document_manager 5处实时索引（create/checkin/tags/release/delete）"
```

---

### Task 6: `workspaces.py`——2 处 ES index 管理

**Files:**
- Modify: `app/routers/workspaces.py`

- [ ] **Step 1: Add import + index calls**

```python
from app.services.indexer_manager import indexer_manager

# create_workspace: after workspace insert
indexer_manager.create_index(workspace_id)

# delete_workspace: before workspace delete
indexer_manager.delete_index(workspace_id)
```

- [ ] **Step 2: Commit**

```bash
git add app/routers/workspaces.py
git commit -m "feat: ES搜索——workspace create/delete 管理 ES index"
```

---

### Task 7: `parts.py` + `documents.py`——搜索端点 ES 优先

**Files:**
- Modify: `app/routers/parts.py`
- Modify: `app/routers/documents.py`

- [ ] **Step 1: Write `_db_search_parts` fallback** (refactor existing LIKE logic into helper function)

- [ ] **Step 2: Modify `search_parts` endpoint**

```python
def search_parts(ws, params, db):
    try:
        keys = es_query_builder.search_parts(ws, params)
        if keys:
            revs = []
            for k in keys:
                pn, ver = k.rsplit("-", 1)
                pr = db.query(PartRevision).filter(...).first()
                if pr:
                    revs.append(map_revision(db, pr))
            return revs
    except Exception:
        pass
    return _db_search_parts(db, ws, params)  # fallback
```

- [ ] **Step 3: Same for `search_documents` in documents.py**

- [ ] **Step 4: Run regression**

Run: `source venv/bin/activate && pytest tests/test_parts_api.py tests/test_documents_api.py -q`
Expected: all pass

- [ ] **Step 5: Commit**

```bash
git add app/routers/parts.py app/routers/documents.py
git commit -m "feat: ES搜索——parts/documents搜索端点ES优先+DB fallback"
```

---

### Task 8: `admin.py`——`POST /admin/index/{ws}` 全量重建

**Files:**
- Modify: `app/routers/admin.py`

```python
@router.post("/admin/index/{ws}")
@router.post("/admin/index/{ws}/", include_in_schema=False)
def reindex_workspace(ws: str, db: Session = Depends(get_db),
                      current_user: Account = Depends(get_current_user)):
    result = indexer_manager.reindex_all(db, ws)
    return result
```

- [ ] **Commit**

```bash
git add app/routers/admin.py
git commit -m "feat: ES搜索——admin POST /index/{ws} 全量重建"
```

---

### Task 9: 测试 + 文件映射更新

**Files:**
- Create: `tests/test_indexer_manager.py`
- Create: `tests/test_es_query_builder.py`
- Modify: `docs/file-mapping.md`

- [ ] **Step 1: test_indexer_manager.py** — Mock ES client, test index CRUD

- [ ] **Step 2: test_es_query_builder.py** — test query DSL construction

- [ ] **Step 3: Update file-mapping.md** — add 2 new pairs

- [ ] **Step 4: Run all tests**

Run: `source venv/bin/activate && pytest tests/ -q`
Expected: all pass

- [ ] **Step 5: Commit**

```bash
git add tests/test_indexer_manager.py tests/test_es_query_builder.py docs/file-mapping.md
git commit -m "test+docs: ES搜索——Mock单元测试+文件映射更新"
```

---

### Task 10: 全量回归 + 重建 + 文档

**Files:**
- Modify: `docs/CHANGELOG.md`

- [ ] **Step 1: Full test suite**

Run: `source venv/bin/activate && pytest tests/ -q`
Expected: 144+ passed

- [ ] **Step 2: Rebuild container**

```bash
cd docdoku-plm-docker && docker compose up -d --build back-py
```

- [ ] **Step 3: Verify ES connectivity from container**

```bash
docker exec docdoku-plm-docker-back-py-1 python -c "from elasticsearch import Elasticsearch; es=Elasticsearch(['es:9200']); print(es.info()['version']['number'])"
```
Expected: `6.6.1`

- [ ] **Step 4: Update CHANGELOG**

- [ ] **Step 5: Commit + push**

```bash
git add docs/CHANGELOG.md && git commit -m "docs: ES搜索——CHANGELOG更新" && git push
```
