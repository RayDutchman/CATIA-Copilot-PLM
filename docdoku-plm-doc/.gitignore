# Jekyll 构建产物（jekyll build 生成，不入库）
app/_site/

# bower 前端依赖（bower install 生成，已在根 .gitignore 覆盖，此处显式声明）
app/bower_components/

# npm 依赖
node_modules/

# Jekyll 缓存
app/.jekyll-cache/
app/.jekyll-metadata

# Ruby gem 本地安装
vendor/

# LaTeX 构建残留
app/_i18n/**/*.log
