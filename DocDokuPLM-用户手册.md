# DocDokuPLM 用户使用手册

> 本手册适用于 DocDokuPLM 2.6.2 版本，面向所有最终用户，涵盖系统的全部功能模块。
>
> 系统访问地址：`http://localhost:8000`

---

## 目录

1. [系统概述](#1-系统概述)
2. [登录与账户](#2-登录与账户)
3. [个人账户设置](#3-个人账户设置)
4. [组织管理](#4-组织管理)
5. [工作空间管理](#5-工作空间管理)
6. [文档管理](#6-文档管理)
7. [产品与零件管理](#7-产品与零件管理)
8. [变更管理](#8-变更管理)
9. [3D 产品结构查看器](#9-3d-产品结构查看器)
10. [实时协作功能](#10-实时协作功能)
11. [权限与角色说明](#11-权限与角色说明)
12. [常见问题](#12-常见问题)

---

## 1. 系统概述

DocDokuPLM 是一个基于 Web 的产品生命周期管理（PLM）系统，提供以下核心能力：

| 功能领域 | 说明 |
|----------|------|
| 文档管理 | 版本控制、检入检出、工作流审批、全文搜索 |
| 产品管理 | 零件版本控制、产品装配树、基线快照、产品实例 |
| 变更管理 | 变更问题、变更申请、变更单的完整生命周期管理 |
| 3D 可视化 | 基于 WebGL 的三维产品结构查看器，支持多人协作 |
| 实时协作 | 在线用户状态、即时聊天、音视频通话、3D 协同会话 |

### 1.1 浏览器要求

推荐使用支持 WebGL 的现代浏览器（Chrome、Firefox、Edge）以获得完整的 3D 功能体验。

### 1.2 界面语言

系统支持以下语言，界面语言会根据浏览器语言设置自动切换：

- 中文（简体）
- English
- Français
- Русский

如需手动切换，请前往[个人账户设置](#3-个人账户设置)。

### 1.3 顶部导航栏

登录后，所有页面顶部显示全局导航栏：

```
[Logo] [我的工作空间 ▾] [当前模块 ▾] [协作者 ▾] [用户名 ▾]
```

- **我的工作空间**：下拉列表中显示你所属的所有工作空间，可快速切换并进入各模块
- **当前模块**：显示当前所在模块名称（文档/产品/变更/工作空间），可切换
- **协作者**：显示当前在线的其他用户
- **用户名**：个人账户、组织管理、退出登录等入口

---

## 2. 登录与账户

### 2.1 登录系统

1. 打开浏览器，访问 `http://localhost:8000`
2. 在登录页面输入**用户名（Login）**和**密码（Password）**
3. 点击**登录**按钮

登录成功后自动跳转到工作空间管理页面。

> **提示**：系统使用 JWT Token 保持登录状态。Token 即将过期时系统会自动静默刷新，无需重新登录。

### 2.2 第三方账号登录（OIDC/OAuth）

如果系统管理员配置了第三方身份提供商（如企业 SSO），可在登录页面点击**使用其他方式登录（Login with...）**，选择对应的提供商完成认证。

### 2.3 注册新账户

> **注意**：是否允许自行注册取决于系统管理员的配置。

1. 在登录页面点击**创建账户（Create account）**
2. 填写以下信息：

| 字段 | 是否必填 | 说明 |
|------|----------|------|
| 用户名（Login） | 必填 | 系统唯一标识，创建后不可修改 |
| 姓名（Name） | 必填 | 显示名称 |
| 邮箱（Email） | 必填 | 用于接收通知和找回密码 |
| 密码（Password） | 必填 | 建议使用字母+数字+符号的组合 |
| 确认密码 | 必填 | 两次输入须一致 |
| 语言 | 可选 | 界面显示语言 |
| 时区 | 可选 | 日期时间的显示时区 |

3. 点击**注册**按钮完成创建

### 2.4 找回密码

1. 在登录页面点击**忘记密码？（Forgot password?）**
2. 输入注册时使用的**邮箱地址**，点击提交
3. 前往邮箱查收重置邮件，点击邮件中的链接
4. 在打开的页面中输入**新密码**（需填写两次）并提交

### 2.5 退出登录

点击右上角用户名下拉菜单，选择**退出登录（Logout）**。

---

## 3. 个人账户设置

点击右上角用户名 → **我的账户（My Account）** 进入账户管理页面。

### 3.1 身份验证

进入账户设置前，系统需要验证你的身份：

- **使用密码登录的用户**：在弹出框中输入当前密码，点击**验证**
- **使用 OIDC 登录的用户**：点击**使用提供商登录**按钮完成验证

### 3.2 修改个人信息

| 字段 | 说明 |
|------|------|
| 姓名（Name） | 修改显示名称 |
| 邮箱（Email） | 修改联系邮箱 |
| 语言（Language） | 修改界面语言（保存后页面自动刷新生效） |
| 时区（Time Zone） | 修改日期/时间显示时区 |

修改后点击**保存（Save）**提交。

### 3.3 修改密码

1. 在账户设置页面点击**修改密码（Change Password）**区域
2. 输入**新密码**
3. 再次输入**确认密码**
4. 点击**保存**

> 如果两次密码不一致，系统会提示错误并拒绝保存。

### 3.4 管理第三方账号绑定

如果系统配置了 OIDC 提供商，可以在账户设置页面绑定或解绑第三方账号，实现单点登录。

---

## 4. 组织管理

点击右上角用户名 → **我的组织（My Organization）** 进入组织管理页面。

**组织（Organization）** 是工作空间的上层概念，用于将多个用户和工作空间归属于同一企业实体。

### 4.1 创建组织

如果你尚未加入任何组织，页面会显示创建表单：

1. 填写**组织名称（Name）**（必填，创建后不可修改）
2. 填写**描述（Description）**（可选）
3. 点击**创建**

创建者自动成为该组织的所有者（Owner）。

### 4.2 编辑组织信息

> 只有组织所有者可执行此操作。

1. 进入**编辑**标签页
2. 修改**描述**内容
3. 点击**保存**

### 4.3 管理组织成员

> 只有组织所有者可管理成员。

进入**成员（Members）**标签页：

**添加成员：**
1. 在输入框中输入对方的**用户名（Login）**
2. 点击**添加**按钮

**删除成员：**
1. 勾选要删除的成员复选框
2. 点击**删除**按钮
3. 在确认对话框中点击**确认**

> 注意：无法删除自己。

**调整成员排列顺序：**
1. 勾选某成员
2. 点击**上移**或**下移**按钮调整其在列表中的位置

### 4.4 删除组织

> 只有组织所有者可执行此操作，且该操作不可撤销。

1. 在编辑页面点击**删除组织（Delete Organization）**
2. 在弹出的确认对话框中点击**确认**

---

## 5. 工作空间管理

点击右上角用户名 → **工作空间管理（Workspaces Administration）** 进入管理页面。

**工作空间（Workspace）** 是 DocDokuPLM 中所有业务数据（文档、零件、产品等）的顶级容器。不同工作空间的数据相互隔离。

### 5.1 创建工作空间

> 只有系统超级管理员可创建工作空间。

1. 点击**新建工作空间**
2. 填写：
   - **工作空间 ID**（必填，全局唯一，创建后不可修改）
   - **描述**（可选）
3. 点击**创建**

创建者自动成为该工作空间的管理员。

### 5.2 编辑工作空间

> 只有工作空间管理员可操作。

进入目标工作空间的**编辑**标签页：
- 修改**描述**
- **冻结/解冻**工作空间（冻结后普通成员无法提交变更）

点击**保存**生效。

### 5.3 管理工作空间用户

> 只有工作空间管理员可操作。

进入目标工作空间的**用户（Users）**标签页：

**添加用户：**
1. 在搜索框中输入用户名并选择
2. 为其分配访问角色
3. 点击**添加**

**移除用户：**
点击用户列表中对应用户旁的**删除**图标。

**管理用户组：**
- 可创建用户组（Group），将多个用户组织在一起
- 在分配文档/零件权限时可以直接指定用户组

### 5.4 配置通知订阅

> 前提：工作空间中至少已创建一个标签（Tag）。

进入工作空间的**通知（Notifications）**标签页：

1. 点击**添加通知**
2. 选择要监控的**标签**
3. 选择触发通知的**事件类型**
4. 设置迭代次数
5. 点击**保存**

当匹配的事件发生时，订阅该通知的用户会收到系统提醒。

### 5.5 仪表盘

进入工作空间的**仪表盘（Dashboard）**标签页，可查看该工作空间的使用统计图表，包括文档总数、零件总数、成员数量等汇总数据。

### 5.6 自定义设置

进入工作空间的**自定义（Customizations）**标签页：

**零件表格列配置：** 自定义零件列表页面中显示的列（如自定义属性字段）。

**Webhook（Hooks）管理：**
1. 点击**添加 Hook**
2. 配置触发条件（事件类型）和回调 URL
3. 保存后，每当指定事件发生，系统会向该 URL 发送 HTTP 请求

### 5.7 系统管理员功能

使用 **admin** 账号登录后，工作空间管理页面会额外显示以下功能：

| 功能 | 说明 |
|------|------|
| 系统仪表盘（Admin Dashboard） | 查看全局存储和用户使用统计 |
| 全局账户管理（Admin Accounts） | 查看、启用/禁用所有用户账户 |
| 全局选项（Admin Options） | 配置是否允许自行注册等系统级参数 |
| OAuth 提供商管理（Admin OAuth） | 添加/编辑/删除第三方 OIDC 登录提供商 |

---

## 6. 文档管理

通过顶部导航的**文档（Documents）** 入口进入文档管理模块。

### 6.1 界面布局

文档管理页面分为三个区域：
- **左侧导航栏**：文件夹树、标签、模板、基线等导航分区
- **主内容区**：文档列表
- **工具栏**：操作按钮（新建、删除、搜索等）

左侧导航栏包含以下分区：

| 分区 | 说明 |
|------|------|
| 文件夹（Folders） | 按树状结构浏览所有文档 |
| 标签（Tags） | 按标签筛选文档 |
| 模板（Templates） | 管理文档模板 |
| 基线（Baselines） | 管理文档基线快照 |
| 已检出（Checked Out） | 查看当前由我检出的文档 |
| 任务（Tasks） | 查看分配给我的工作流审批任务 |
| 搜索（Search） | 高级搜索 |

### 6.2 创建文档

1. 导航到目标文件夹
2. 点击工具栏中的**新建文档（New Document）**按钮
3. 在弹出的对话框中填写：

| 字段 | 是否必填 | 说明 |
|------|----------|------|
| 文档编号（Document ID） | 必填 | 在工作空间内唯一，创建后不可修改 |
| 标题（Title） | 可选 | 文档的显示名称 |
| 模板（Template） | 可选 | 套用预定义的属性结构 |
| 工作流（Workflow） | 可选 | 指定审批流程 |
| 描述（Description） | 可选 | 文档描述 |
| 标签（Tags） | 可选 | 可选择多个标签 |

4. 点击**创建**

### 6.3 检出文档（Check Out）

文档必须**先检出才能编辑**，检出期间其他用户只能查看。

1. 在文档列表中找到目标文档
2. 点击文档行右侧的**检出**按钮（或在文档详情中操作）
3. 文档状态变为"已检出（Checked Out）"，文档旁显示锁定图标

### 6.4 编辑文档内容

文档检出后，可以：
- **上传文件**：在文档详情中点击**添加文件（Add File）**，上传附件
- **修改属性**：编辑文档的自定义属性字段
- **修改描述/标题**：在详情页直接修改

### 6.5 检入文档（Check In）

完成编辑后，将修改提交为新版本：

1. 点击文档的**检入（Check In）**按钮
2. 可选填写**迭代说明（Iteration Note）**，记录本次修改内容
3. 点击**确认**

检入后文档释放锁定，其他用户可以查看最新版本。

### 6.6 撤销检出（Undo Check Out）

放弃本次编辑，恢复到检出前的状态：

1. 点击文档的**撤销检出（Undo Check Out）**按钮
2. 在确认对话框中点击**确认**

> **警告**：撤销检出会**永久丢失**本次检出后的所有修改，操作不可撤销。

### 6.7 创建新版本（New Version）

在不修改现有版本的情况下，基于当前版本创建一个新的独立版本分支：

1. 在文档详情页点击**新版本（New Version）**
2. 系统创建新版本（如从 A 升至 B），并自动检出新版本

### 6.8 管理文件夹

**创建文件夹：**
1. 在左侧文件夹树中，右键点击目标位置或点击**新建文件夹**按钮
2. 输入文件夹名称
3. 按回车或点击确认

**重命名文件夹：**
点击文件夹旁的**编辑**图标，修改名称后保存。

**删除文件夹：**
点击文件夹旁的**删除**图标（只有空文件夹才可删除）。

文件夹支持多级嵌套，可将文档拖拽到不同文件夹中进行整理。

### 6.9 标签管理

**创建标签：**
1. 在左侧**标签**分区点击**新建标签（New Tag）**
2. 输入标签名称（颜色由系统自动分配）
3. 点击确认

**删除标签：**
点击标签旁的**删除**图标（删除标签不会删除关联的文档）。

**给文档添加标签：**
在文档详情页的**标签**区域，选择要关联的标签并保存。

### 6.10 文档模板

模板用于预定义文档的属性结构，方便快速创建同类文档。

**创建模板：**
1. 进入左侧**模板（Templates）**分区，点击**新建模板**
2. 填写：
   - **模板 ID**（必填，唯一）
   - **模板名称**（可选）
   - **文件掩码（File Mask）**（可选，限制允许上传的文件类型，如 `*.pdf`）
3. **添加自定义属性**：
   - 点击**添加属性**
   - 设置属性名称、类型（文本/日期/数字/布尔值/列表值等）、是否必填
   - 可添加多个属性
4. 点击**保存**

**编辑/删除模板：**
在模板列表中点击对应行进行操作。

### 6.11 文档基线

基线用于固化某一时刻的文档集合快照，后续即使文档内容变化，基线记录的内容不受影响。

**创建基线：**
1. 进入左侧**基线（Baselines）**分区，点击**新建基线**
2. 填写**基线名称**和**描述**
3. 选择基线类型：
   - **LATEST**：捕获所选文档的最新版本
   - **RELEASED**：仅捕获已发布（Released）状态的版本
4. 从文档树中勾选要加入基线的文档
5. 点击**创建**

> **注意**：不能创建空的文档基线（至少需选择一个文档）。

### 6.12 高级搜索

进入左侧**搜索（Search）**分区，可组合以下条件进行精确搜索：

| 搜索条件 | 说明 |
|----------|------|
| 文档 ID | 支持精确匹配或模糊匹配 |
| 标题 | 文档标题关键词 |
| 类型 | 文档类型 |
| 关键词（Content） | 在文档内容中全文搜索 |
| 作者 | 创建者用户名 |
| 创建日期范围 | 指定起止日期 |
| 修改日期范围 | 指定起止日期 |
| 自定义属性值 | 按模板中定义的属性过滤 |

填写条件后点击**搜索**，结果显示在列表中，可进一步操作。

### 6.13 工作流任务

如果文档绑定了工作流，相关审批任务会出现在左侧**任务（Tasks）**分区：

1. 点击任务查看详情（任务说明、关联文档、截止日期等）
2. 选择处理意见：
   - **批准（Approve）**：同意并推进工作流进入下一步
   - **拒绝（Reject）**：驳回并可附上拒绝原因
3. 提交处理结果

---

## 7. 产品与零件管理

通过顶部导航的**产品（Products）** 入口进入产品管理模块。

### 7.1 界面布局

左侧导航栏包含：

| 分区 | 说明 |
|------|------|
| 产品（Products） | 产品列表 |
| 配置（Configurations） | 产品配置管理 |
| 基线（Baselines） | 产品基线 |
| 产品实例（Product Instances） | 按序列号管理的产品实例 |
| 零件（Parts） | 零件列表（含高级筛选） |
| 标签（Tags） | 零件标签 |
| 已检出（Checked Out） | 当前由我检出的零件 |
| 任务（Tasks） | 工作流审批任务 |
| 零件模板（Part Templates） | 零件属性模板 |
| 零件搜索（Parts Search） | 多条件高级搜索 |

### 7.2 创建零件

1. 进入**零件**分区，点击**新建零件（New Part）**
2. 填写：

| 字段 | 是否必填 | 说明 |
|------|----------|------|
| 零件编号（Part Number） | 必填 | 在工作空间内唯一，创建后不可修改 |
| 零件名称（Name） | 可选 | 显示名称 |
| 标准件（Standard Part） | 可选 | 勾选表示此零件为标准件（外购件） |
| 描述（Description） | 可选 | |
| 模板（Template） | 可选 | 套用预定义属性结构 |
| 工作流（Workflow） | 可选 | 指定审批流程 |

3. 点击**创建**

### 7.3 零件检出 / 检入 / 撤销检出

操作与文档相同（参见第 6.3、6.5、6.6 节）。

### 7.4 创建零件新版本

在零件详情页点击**新版本（New Version）**，基于当前版本创建新的版本分支。

### 7.5 批量导入零件

1. 在零件列表页点击**导入（Import）**按钮
2. 上传 CSV 或 Excel 格式的零件数据文件
3. 系统批量创建零件

### 7.6 创建产品

> **前提**：工作空间中至少已存在一个零件。

1. 进入**产品（Products）**分区，点击**新建产品（New Product）**
2. 填写：
   - **产品 ID**（必填，工作空间内唯一）
   - **描述**（可选）
   - **根零件（Root Part）**（必填，选择作为产品顶层节点的零件）
3. 点击**创建**

创建产品后，可点击产品进入详情页，点击**查看产品结构（View Product Structure）**链接进入 3D 产品结构查看器。

### 7.7 产品配置（Configurations）

配置（Configuration）用于管理零件的**有效性规则**，定义在特定条件下哪些零件版本是有效的。

**创建配置：**
1. 进入**配置**分区，点击**新建配置**
2. 填写配置名称和描述
3. 定义有效性条件
4. 保存

### 7.8 创建产品基线

基线用于固化产品结构的快照，记录某一时刻每个零件使用的确切版本。

> **前提**：工作空间中至少已存在一个产品。

1. 进入**基线（Baselines）**分区，点击**新建基线**
2. 填写**基线名称**和**描述**
3. 选择基线类型：
   - **LATEST**：使用各零件的最新版本
   - **RELEASED**：仅使用已发布（Released）状态的版本
4. 在产品结构树中，为每个节点选择具体的零件版本
5. 点击**创建**

### 7.9 创建产品实例

产品实例用于追踪每个**具体物理产品**的状态（序列号管理）。

> **前提**：工作空间中至少已存在一个产品基线。

1. 进入**产品实例（Product Instances）**分区，点击**新建实例**
2. 填写：
   - **序列号（Serial Number）**（必填）
   - **基于的基线（Baseline）**（必填，从列表中选择）
3. 点击**创建**

**批量导入产品实例：**
点击**导入**按钮，上传包含序列号数据的文件批量创建实例。

### 7.10 零件模板

与文档模板类似，零件模板用于预定义零件的自定义属性结构。

**创建零件模板：**
1. 进入**零件模板（Part Templates）**分区，点击**新建模板**
2. 填写模板 ID、名称
3. 添加自定义属性（类型、名称、是否必填等）
4. 设置文件掩码（限制允许上传的 CAD 文件类型）
5. 保存

### 7.11 高级零件搜索

进入**零件搜索（Parts Search）**分区，可使用可视化查询构建器（Query Builder）组合以下条件：

| 搜索条件 | 说明 |
|----------|------|
| 零件编号 | 支持通配符 `%`（如 `BOLT%` 匹配所有以 BOLT 开头的零件） |
| 零件名称 | 关键词匹配 |
| 版本（Version） | 指定版本号 |
| 迭代次数（Iteration） | 指定迭代编号 |
| 作者 | 创建者用户名 |
| 创建日期范围 | |
| 修改日期范围 | |
| 自定义属性值 | 按模板属性过滤 |

---

## 8. 变更管理

通过顶部导航的**变更（Changes）** 入口进入变更管理模块。

### 8.1 变更管理流程概述

DocDokuPLM 的变更管理遵循以下三级流程：

```
发现问题
   ↓
变更问题（Change Issue）— 记录已发现的问题或缺陷
   ↓
变更申请（Change Request）— 针对问题提出的变更需求
   ↓
变更单（Change Order）— 已批准并执行的正式变更
   ↓
里程碑（Milestone）— 跟踪变更的时间节点目标
```

### 8.2 界面布局

左侧导航栏包含：

| 分区 | 说明 |
|------|------|
| 工作流（Workflows） | 审批工作流模型管理 |
| 里程碑（Milestones） | 项目里程碑 |
| 变更问题（Issues） | 变更问题列表 |
| 变更申请（Requests） | 变更申请列表 |
| 变更单（Orders） | 变更单列表 |
| 任务（Tasks） | 我的工作流审批任务 |

### 8.3 管理工作流角色

在创建工作流之前，需先定义**角色（Roles）**（角色代表某类审批人员，如"质量工程师"、"主设计师"等）。

1. 点击变更管理页面顶部的**角色（Roles）**按钮，打开角色管理对话框
2. **创建角色**：
   - 在输入框中输入角色名称（不能与已有角色重复）
   - 点击**添加**按钮
3. **为角色设置默认执行人**：
   - 从**默认指定用户（Default Assigned Users）**下拉框中选择用户
   - 从**默认指定用户组（Default Assigned Groups）**下拉框中选择用户组
4. **删除角色**：点击角色旁的删除图标
   > 注意：正在被工作流模型使用的角色**不能删除**
5. 点击**保存（Save）**提交所有更改

### 8.4 创建工作流模型

工作流模型定义了文档或零件的审批流程，可由多个顺序或并行的**活动（Activity）** 组成，每个活动包含一个或多个**任务（Task）**。

1. 进入**工作流（Workflows）**分区，点击**新建工作流**
2. 在编辑器顶部输入**工作流名称**（必填）
3. **添加活动：**
   - 点击**添加活动（Add Activity）**按钮
   - 输入活动名称
   - 选择活动类型：
     - **顺序（Sequential）**：活动中的所有任务必须按顺序完成
     - **并行（Parallel）**：活动中的任务可同时进行，指定需要多少人批准才算通过
4. **在活动中添加任务：**
   - 点击活动区块中的**添加任务（Add Task）**按钮
   - 输入任务名称（点击名称可折叠/展开任务详情）
   - 输入**任务说明（Instructions）**（告知执行人需要做什么）
   - 从**角色**下拉框中选择执行角色（若没有合适的角色，可点击**新建角色**临时创建）
   - 如需删除任务，点击任务旁的**垃圾桶**图标
5. 设置**最终生命周期状态（Final Lifecycle State）**（必填，如"已批准"、"已发布"等）
6. 点击**保存（Save）**

> **业务规则：**
> - 工作流至少需要包含一个活动和一个任务
> - 必须填写最终状态名称
> - 角色必须事先在角色管理中创建（或在此处临时创建）

**复制已有工作流：** 在工作流列表中点击某工作流旁的**复制**图标，以其为模板创建新工作流。

**删除工作流：** 点击**删除**图标（只有未被任何文档/零件使用的工作流可以删除）。

### 8.5 创建里程碑

里程碑用于设定项目的关键时间节点，变更申请和变更单可与里程碑关联以追踪进度。

1. 进入**里程碑（Milestones）**分区，点击**新建里程碑**
2. 填写：
   - **标题（Title）**（必填）
   - **描述（Description）**（可选）
   - **到期日期（Due Date）**（必填）
3. 点击**创建**

**编辑里程碑：**
点击里程碑进入编辑弹窗，可修改标题、描述和日期。弹窗中还会显示关联此里程碑的变更申请和变更单列表（只读）。

### 8.6 创建变更问题（Change Issue）

变更问题用于记录在产品研发或生产过程中发现的**缺陷、错误或需要关注的事项**。

1. 进入**变更问题（Issues）**分区，点击**新建问题**
2. 填写基本信息：

| 字段 | 是否必填 | 说明 |
|------|----------|------|
| 名称（Name） | 必填 | 问题简要描述 |
| 描述（Description） | 可选 | 详细说明 |
| 优先级（Priority） | 可选 | Emergency（紧急）/ High（高）/ Medium（中）/ Low（低） |
| 类别（Category） | 可选 | Electrical（电气）/ Mechanical（机械）/ Software（软件）/ Other（其他） |
| 负责人（Assignee） | 可选 | 指定处理该问题的用户 |
| 里程碑（Milestone） | 可选 | 关联到期节点 |

3. 切换到**受影响文档（Affected Documents）**标签页，搜索并添加受影响的文档
4. 切换到**受影响零件（Affected Parts）**标签页，搜索并添加受影响的零件
5. 点击**创建**

**编辑变更问题：** 在列表中点击问题名称进入编辑弹窗，可修改所有字段并管理标签。

### 8.7 创建变更申请（Change Request）

变更申请用于针对已知问题**正式提出变更需求**。

1. 进入**变更申请（Requests）**分区，点击**新建申请**
2. 填写基本信息（字段与变更问题相同）
3. 切换到**受影响文档**和**受影响零件**标签页，添加关联项
4. 切换到**关联的变更问题（Addressed Issues）**标签页，将此申请与上游变更问题关联
5. 点击**创建**

### 8.8 创建变更单（Change Order）

变更单是**已正式批准并将执行**的变更指令。

1. 进入**变更单（Orders）**分区，点击**新建变更单**
2. 填写基本信息（字段与变更问题相同）
3. 切换到**受影响文档**和**受影响零件**标签页，添加关联项
4. 切换到**关联的变更申请（Addressed Requests）**标签页，将此变更单与上游变更申请关联
5. 点击**创建**

### 8.9 处理变更工作流任务

若变更问题/申请/变更单绑定了工作流，审批任务会出现在**任务（Tasks）**分区：

1. 点击任务查看详情
2. 阅读任务说明和关联内容
3. 选择处理结果：
   - **批准（Approve）**：同意变更，工作流推进到下一活动
   - **拒绝（Reject）**：驳回变更，可附上拒绝原因

---

## 9. 3D 产品结构查看器

### 9.1 进入查看器

在产品管理页面选择一个产品，点击**查看产品结构（View Product Structure）**，进入 3D 可视化界面。

也可以从 BOM 或零件详情页直接进入特定节点的 3D 视图。

### 9.2 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  [配置规格 ▾]  [BOM 模式]  [3D 场景模式]  [协作会话]     │ ← 顶部工具栏
├──────────────┬──────────────────────────┬────────────────┤
│              │                          │  导航控制      │
│  零件树       │   3D 场景 / BOM 表格     │  视图模式      │
│  (左侧)      │   (主内容区)             │  变换控制      │
│              │                          │  爆炸视图      │
│              │                          │  剪切视图      │
│              │                          │  测量工具      │
│              │                          │  图层管理      │
│              │                          │  标记管理      │
│              │                          │  选项          │
└──────────────┴──────────────────────────┴────────────────┘
```

### 9.3 零件树操作

左侧**零件树**显示产品的层级装配结构：

- **展开/折叠**：点击节点旁的展开箭头查看子装配
- **定位到 3D**：点击某个零件节点，3D 视图中对应部件会高亮显示
- **搜索零件**：在零件树顶部的搜索框中输入零件编号（支持通配符 `%`），按回车搜索并定位

### 9.4 BOM 物料清单

点击顶部工具栏的 **BOM** 按钮切换到 BOM 表格视图，以表格形式显示产品装配中所有零件的清单信息。

**BOM 操作工具栏**（选中零件后显示）：

| 按钮 | 功能 | 前提条件 |
|------|------|----------|
| 检出（Check Out） | 检出选中零件 | 零件未被检出 |
| 撤销检出（Undo Check Out） | 撤销检出 | 零件已由我检出 |
| 检入（Check In） | 检入零件并填写迭代说明 | 零件已由我检出 |
| 编辑访问控制（Edit ACL） | 修改零件权限 | 仅创建者或管理员 |
| 级联检出（Cascade Check Out） | 递归检出该零件及所有子零件 | |
| 级联撤销检出 | 递归撤销检出 | |
| 级联检入（Cascade Check In） | 递归检入，填写统一迭代说明 | |

> 已发布（Released）或已废弃（Obsolete）状态的零件不显示检出按钮。

### 9.5 3D 场景操作

#### 鼠标基本操作

| 操作 | 效果 |
|------|------|
| 左键拖拽 | 旋转视角 |
| 右键拖拽 | 平移视角 |
| 滚轮 | 缩放 |
| 单击零件 | 选中该零件 |
| 双击空白处 | 在该位置创建 3D 标记 |

#### 导航控制（Navigation）

| 按钮 | 功能 |
|------|------|
| 飞向选中（Fly To） | 镜头平滑飞入当前选中的零件 |
| 注视（Look At） | 将选中零件置于视图中心 |
| 最佳视角（Best Fit） | 未选中任何零件时，调整视角以完整显示整个产品 |
| 重置镜头（Reset Camera） | 恢复到初始视角 |

#### 视图模式（View Modes）

| 模式 | 说明 |
|------|------|
| 轨道模式（Orbit Mode） | 默认模式，绕中心点旋转查看 |
| 追踪模式（Tracking Mode） | 轨迹球控制，更自由的旋转 |
| 飞行模式（Flying Mode） | 第一人称视角，鼠标锁定，键盘 WASD 前进后退 |

#### 变换控制（Transform）

> 前提：零件必须已被检出。

1. 选中已检出的零件
2. 在**变换控制**面板中选择操作类型（平移/旋转/缩放）
3. 在 3D 场景中拖动出现的操作手柄
4. 如需取消变换，点击**取消变换（Cancel Transformation）**按钮

#### 爆炸视图（Explode View）

拖动**爆炸滑块（Explode Slider）**，零件将沿各自轴向向外展开，便于查看内部结构。滑块拉到最左恢复正常。

#### 剪切视图（Clipping）

拖动**剪切滑块（Clipping Slider）**，从前往后切开产品，显示剖面内部结构。滑块拉到最右恢复正常。

#### 测量工具（Measure）

1. 打开**测量开关（Measure Toggle）**进入测量模式
2. 在 3D 场景中依次点击两个测量点
3. 系统显示两点之间的距离
4. 点击**清除测量（Clear Measures）**删除所有测量标注

#### 图层管理（Layers）

图层用于对场景中的零件进行分组显示/隐藏控制：

1. 在**图层（Layers）**面板中查看已定义的图层列表
2. 切换图层旁的显示/隐藏开关
3. 可在图层上点击**显示此图层**或**仅显示此图层**快速操作

#### 3D 标记（Markers）

3D 标记用于在产品的特定位置添加注释或说明：

**创建标记：**
1. 在 3D 场景中双击目标位置，弹出创建对话框
2. 输入**标记名称（Name）**
3. 输入**标记描述（Description）**
4. 点击**保存**，标记以图标固定在 3D 空间中

**管理标记：**
- **缩小标记（Zoom Less）**：减小所有标记图标的显示尺寸
- **放大标记（Zoom More）**：增大所有标记图标的显示尺寸
- **切换标记显示（Marker State）**：批量显示/隐藏所有标记
- 点击某标记图标可查看其详情

#### 选项（Options）

| 选项 | 说明 |
|------|------|
| 显示网格（Grid） | 切换地面参考网格的显示 |
| 截图（Screenshot） | 对当前 3D 视图截图并保存为图片文件 |
| 显示已编辑零件（Show Edited Meshes） | 用橙色高亮显示当前已被检出/正在编辑的零件 |

### 9.6 配置规格（Config Spec）

点击顶部工具栏的**配置规格（Config Spec）**下拉菜单，可切换查看产品在不同**基线**或**配置**下的结构状态：

- **最新（Latest）**：显示所有零件的最新迭代版本
- **已发布（Released）**：仅显示已发布版本的零件
- **指定基线**：显示该基线创建时固化的产品结构

### 9.7 书签与分享

当前 3D 视图的相机位置（角度、位置、目标点）会自动编码到 URL 中，可将当前 URL 分享给他人，对方打开后将看到完全相同的视角。

---

## 10. 实时协作功能

DocDokuPLM 内置实时协作工具，无需安装任何插件即可使用。

### 10.1 查看在线协作者

点击顶部导航栏的**协作者（Coworkers）**下拉菜单，查看当前在线的工作空间成员列表及其状态：

| 状态 | 含义 |
|------|------|
| 在线（Online） | 用户正在使用系统 |
| 离开（Away） | 用户已有一段时间未操作 |
| 忙碌（Busy） | 用户正在进行协作会话等活动 |

### 10.2 即时聊天

1. 点击用户名旁的**聊天**图标，或从协作者列表中选择用户
2. 弹出聊天窗口
3. 在输入框中输入消息，按回车发送

### 10.3 音视频通话（WebRTC）

1. 从协作者列表或用户弹出卡片中点击**视频通话**图标
2. 系统向对方发送通话邀请
3. 对方收到通知后可选择**接听**或**拒绝**
4. 接听后建立点对点音视频连接
5. 通话中点击**挂断**按钮结束通话

> **注意**：WebRTC 通话需要浏览器的摄像头/麦克风权限，浏览器会弹出权限请求，需点击**允许**。

### 10.4 3D 协同查看会话

多人可以实时共享同一 3D 产品视图，支持一人主导、其他人跟随观看。

#### 创建协作会话（主持人操作）

1. 在 3D 产品结构查看器中，点击右侧面板的**协作（Collaborative）**区域
2. 点击**创建协同会话（Create Session）**
3. 系统生成唯一的**会话密钥（Room Key）**，页面 URL 自动更新为包含密钥的链接
4. 邀请其他用户加入：
   - 在协作面板的用户列表中找到目标用户
   - 点击**邀请（Invite）**按钮发送邀请通知

#### 加入协作会话（参与者操作）

- 收到邀请通知后，点击**接受（Accept）**加入会话
- 或者直接打开包含会话密钥的 URL

加入后，参与者的视角将**跟随主持人**，无法自行操作 3D 场景。

#### 协作会话管理（主持人专属）

| 操作 | 说明 |
|------|------|
| 授权（Give Hand） | 将 3D 场景的控制权转交给某个参与者 |
| 撤销授权（Take Hand） | 将控制权收回 |
| 踢出（Kick） | 将某参与者强制移出会话 |
| 撤销邀请（Withdraw Invitation） | 取消对仍未接受邀请用户的邀请 |
| 退出（Exit） | 主持人退出即**解散整个会话**，所有参与者同时退出 |

#### 退出会话（参与者操作）

点击**退出（Exit）**仅退出自己，不影响主持人和其他参与者。

> **业务规则：**
> - 同一时间只有一人能控制 3D 场景（主持人或被授权的参与者）
> - 主持人退出会话后，整个会话立即解散
> - 参与者在会话期间无法操作变换、测量、标记等工具（只读）

---

## 11. 权限与角色说明

### 11.1 系统角色层级

```
超级管理员（Root Admin）
   ↓
工作空间管理员（Workspace Admin）
   ↓
工作空间普通成员（Member）
   ↓
只读成员（Read-Only Member）
```

| 角色 | 权限说明 |
|------|----------|
| 超级管理员 | 创建/删除工作空间；管理全局账户和 OAuth 配置；查看系统统计 |
| 工作空间管理员 | 管理工作空间内的用户、通知、模板、工作流；可检出任何人的文档/零件 |
| 普通成员 | 创建、编辑（检出/检入）、删除自己有权限的文档和零件 |
| 只读成员 | 仅可查看，不能编辑或创建任何内容 |

### 11.2 文档/零件级权限（ACL）

除工作空间级别的角色外，每个文档和零件还可以单独设置访问控制列表（ACL）：

1. 在文档/零件详情页点击**编辑访问控制（Edit ACL）**
2. 为指定用户或用户组分配权限级别：
   - **完全控制（Full Access）**：可读写
   - **只读（Read Only）**：仅可查看

### 11.3 检出限制规则

- 同一时间，一个文档/零件**只能被一个用户检出**
- 工作空间管理员可以**强制检入**他人检出的文档/零件（紧急情况下使用）
- 已发布（Released）或已废弃（Obsolete）状态的零件不能检出

---

## 12. 常见问题

**Q：为什么我无法编辑某个文档？**
A：文档需要先"检出"才能编辑。如果检出按钮是灰色的，可能是该文档已被其他用户检出，或者你没有编辑权限。

**Q：检出的文档显示有锁，我能查看吗？**
A：可以查看最后一次检入的版本内容，但无法编辑。只有检出人才能看到最新的修改草稿。

**Q：如何查看文档/零件的历史版本？**
A：在文档/零件详情页中，可以查看所有历史迭代记录，点击某个迭代可查看该版本的内容和文件。

**Q：工作流是什么？我需要配置它吗？**
A：工作流是可选的审批流程。如果你的文档或零件需要经过多人审批才能发布，则需要绑定工作流。如果不需要审批流程，可以不使用工作流。

**Q：基线和版本有什么区别？**
A：版本（Version）是单个文档/零件的迭代记录；基线（Baseline）是某一时刻多个文档或零件状态的集合快照，用于确保一组相关内容的一致性。

**Q：3D 查看器加载很慢怎么办？**
A：3D 模型文件可能较大，加载时间取决于网络速度和文件大小。可以使用图层管理功能隐藏暂时不需要查看的零件，减少渲染负担。

**Q：协作会话结束后，其他人能继续查看 3D 模型吗？**
A：可以。会话解散后，所有参与者仍可以独立打开 3D 查看器查看产品，但不再实时同步。

**Q：如何快速找到某个特定零件？**
A：在 3D 查看器左侧零件树的搜索框中输入零件编号（支持 `%` 通配符），或者在产品管理的"零件搜索"分区使用高级查询构建器。

**Q：变更问题、变更申请和变更单有什么区别，必须三个都创建吗？**
A：不是必须的，三者是递进关系：问题记录已知缺陷，申请提出解决方案，变更单是执行指令。可以根据实际需要只使用其中一个或两个。

---

*本手册基于 DocDokuPLM 2.6.2 版本编写。如有功能差异，请以实际界面为准。*
